---
description: "Run drup upgrade pipeline commands"
---

Use the drup MCP server to run Drupal upgrade pipeline commands.

Available tools: init, scan, fix, contrib, validate, upgrade-core, apply-patch, report, preflight, install, sync, upgrade.

Example: `/drup validate /path/to/project`
