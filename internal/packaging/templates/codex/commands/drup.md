---
description: Run the drup Drupal upgrade automation workflow
argument-hint: [PROJECT_PATH=]
---

Load the `drup` skill and run its complete workflow for $PROJECT_PATH. If it is omitted, use the current project. Start by reporting the detected project path and wait for confirmation before any mutation.
